Welcome to the OpenNMS @appversion@ installer!

Before installation, make sure you've got PostgreSQL (8.3 or higher)
and a Sun-compatible JDK (7 or higher) installed.

To install OpenNMS @appversion@ just run the installer as a user with administrator privileges from the command line:

    java -jar standalone-opennms-installer-@appversion@.jar

For details on installation, please see:

  http://www.opennms.org/wiki/QuickStart

